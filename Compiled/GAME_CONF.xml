<?xml version="1.0" ?>

<conf>
    <!-- Contiene la lista de niveles del juego, cada nivel tiene un path con su
         ubicacion relativa a la carpeta Levels. -->
    <campaing>
        <!-- there is a maximun of 20 levels -->
        <level path="lvl_1.xml" />
    </campaing>
    
    <!-- Warning: Set verbose to "true" will increase the output file's weight
         while the game run. -->
    <debuger verbose="true" />
    
    <graphics>
        <shaderheightmap active="true" />
    </graphics>
    
</conf>


